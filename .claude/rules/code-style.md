# Code style rules

- Follow the style already present in the repository.
- Prefer simple control flow and explicit variable names.
- Keep modules small and cohesive.
- Avoid deep nesting when an early return is clearer.
- Minimize inline comments; use them only when intent is not obvious.
- Do not add heavy abstractions too early.
- Keep formatting consistent with project tooling if present.
