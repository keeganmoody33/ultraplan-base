# Workflow rules

- For ambiguous requests, state the likely interpretation before making changes.
- For multi-step tasks, present a short plan first.
- For risky changes, ask for confirmation before destructive actions.
- Prefer incremental commits in spirit, even if not actually committing.
- When fixing bugs, identify root cause, not just the symptom.
- When adding files, keep naming simple and predictable.
